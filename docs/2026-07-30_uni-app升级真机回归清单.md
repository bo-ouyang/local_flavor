# uni-app / DCloud 5.15 真机回归清单

> 状态：BAS-04 实现和真实 Actions 验证完成，PR #5 待合并。最终 Actions run [`30598504466`](https://github.com/bo-ouyang/local_flavor/actions/runs/30598504466) 已在 head `c5be87e` 上通过，4 个 jobs 均为 success；精确 Node.js `22.23.2` / npm `10.9.8`、微信小程序与 H5 双端构建和真实依赖审计均已通过。
>
> 微信真机回归仍未完成，本清单保持全部未勾选；不得用 H5、类型检查、构建或 Actions 成功代替真机结果，也不可宣称上线验收完成。

## 1. 升级基线

| 项目 | 版本 |
|---|---|
| Node.js / npm | `22.23.2` / `10.9.8` |
| DCloud Vue 3 编译链 | `3.0.0-5010520260709002`（编译器 5.15） |
| `@dcloudio/types` | `3.4.31`（`uni-app` 精确 peer） |
| Vue / Vite / plugin-vue | `3.4.21` / `5.2.8` / `5.2.4` |
| TypeScript / vue-tsc | `5.4.5` / `3.3.8` |

静态检查已经确认：

- 微信登录继续使用 `uni.login({ provider: 'weixin' })` 并校验 `code`。
- 定位继续使用 `uni.getLocation({ type: 'gcj02' })`，首次登录拒绝定位不会阻断登录。
- 商品与社区图片继续通过 `uni.uploadFile` 携带 Bearer Token，并解析统一响应信封。
- 聊天继续使用 `uni.connectSocket` 返回的 `SocketTask` 绑定 open/message/close/error，失败时保留 REST 轮询降级。
- ECharts Canvas2D 组件的 selector query、`setCanvasCreator`、初始化、触摸和销毁代码通过类型检查与双端构建；但当前没有已注册页面引用该组件，不能宣称真机图表已经验证。

## 2. 测试记录

- [ ] PR/commit：`________________`
- [ ] 测试日期与测试人：`________________`
- [ ] 微信开发者工具版本：`________________`
- [ ] 微信基础库版本：`________________`
- [ ] 真机型号 / OS / 微信版本：`________________`
- [ ] 后端环境与 API 域名：`________________`
- [ ] 已清除旧小程序缓存和登录 Token 后重新编译导入。

## 3. 微信登录

- [ ] 未登录进入受保护操作时正确跳转登录页。
- [ ] 点击微信登录可获得临时 code，后端登录成功并持久化 Token。
- [ ] 登录成功后用户资料、头像和地区正常刷新。
- [ ] code 无效、网络失败或后端拒绝时显示失败提示，不残留伪登录状态。
- [ ] 已登录用户重复进入登录链路不会产生重复请求或页面循环。

## 4. 定位

- [ ] 首次登录允许定位后，GCJ-02 坐标成功同步为省市。
- [ ] 首次登录拒绝定位后仍可完成登录，页面无阻塞或白屏。
- [ ] 个人页手动更新位置成功，并展示后端返回的省市。
- [ ] 系统定位关闭、超时和弱网情况下错误提示可理解。
- [ ] 地区 30 天冷却限制由后端拒绝时，前端展示真实错误且不伪造成功状态。

## 5. 图片选择与上传

- [ ] 商品发布从相册选择压缩图片并成功上传、预览、删除。
- [ ] 商品发布从相机拍摄并成功上传。
- [ ] 商品最多 3 张、社区最多 9 张的选择限制有效。
- [ ] 上传请求携带当前 Bearer Token，失效 Token 不会被当作成功。
- [ ] 服务端拒绝格式/大小、网络中断和 JSON 异常时显示失败提示，已成功图片不丢失。
- [ ] 社区发布页完成图片上传与提交，返回列表后图片 URL 可访问。

## 6. WebSocket 与轮询降级

- [ ] 进入有效会话后 WebSocket 成功连接，页面状态显示已连接。
- [ ] 双机互发文本、图片和商品卡片，消息顺序与去重正常。
- [ ] 新消息触发已读 ACK，返回会话列表后未读数正确。
- [ ] 小程序进入后台会主动关闭连接，回到前台可恢复连接。
- [ ] 临时断网或服务端关闭 WebSocket 后，REST 轮询继续收到消息。
- [ ] Token 无效、会话无权限时不会无限重连或泄露聊天数据。

## 7. ECharts

当前没有注册页面引用 `src/components/l-echart/l-echart.vue`，以下项目保持未完成，直至产品页面实际接入该组件：

- [ ] Canvas2D 在微信真机创建成功，尺寸与 DPR 正确。
- [ ] 图表数据渲染、更新和页面返回后的 dispose 正常。
- [ ] 触摸移动、缩放和点击事件在真机正确映射。
- [ ] 多次进入/退出页面没有重复实例、内存持续增长或控制台错误。

## 8. 核心页面回归与验收

- [ ] 首页、今日上新、详情、发布、社区、聊天、交换和个人页均可打开。
- [ ] TabBar、导航返回、下拉刷新和登录后重定向正常。
- [ ] 微信开发者工具无新增运行时错误；真机日志已保存到 PR 证据。
- [ ] `dist/build/mp-weixin` 使用正式 AppID 导入并完成预览/真机调试。
- [ ] 上述阻断项全部通过；失败项有复现步骤、设备信息和日志。

只有微信登录、定位、上传、WebSocket 以及实际启用后的 ECharts 真机项完成后，BAS-04 才能从“实现完成”更新为“真机验收完成”。
